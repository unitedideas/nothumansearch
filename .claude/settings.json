{
  "hooks": {
    "post-stop": [
      {
        "name": "commit-discipline",
        "command": "bash",
        "script": "if git status --porcelain | grep -q .; then git add -A && git commit -m 'Auto-commit: local changes from session end' && git push origin HEAD; else exit 0; fi"
      }
    ]
  }
}
