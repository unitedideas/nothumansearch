# nothumansearch Agent Memory

Business-specific agents write scoped learning proposals here. Shared QLimit agents do not write directly into this business memory without an explicit business-scoped task.
