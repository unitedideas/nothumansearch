# Hermes Atlas Referrer Agent Directory Packet - 2026-05-21

Automation: `business-marketer-not-human-search`
Scope: business-local marketing scout artifact only. No outreach, public post, directory submission, account creation, browser action, product-code edit, deploy, checkout completion, or crawl was performed.

## Evidence

- Public stats: `total_sites=4171`, `avg_score=35`, `top_category=developer`.
- Public categories: `developer=1228`, `ai-tools=897`, `other=777`, `data=402`, `finance=194`, `productivity=174`, `ecommerce=148`, `communication=118`, `security=114`, `health=58`, `jobs=27`, `education=21`, `news=12`, `spam=1`.
- Aggregate traffic, 168 hours: `/=3409`, `/badge/xquik.com.svg=2217`, `/.well-known/commerce.json=1522`, `/site/xquik.com=736`, `/.well-known/ai-plugin.json=700`, `/llms.txt=455`, `/openapi.yaml=425`, `/api/v1/catalog=328`, `/robots.txt=307`, `/api/v1/checkout=299`, `/api/v1/quote=299`, `/api/v1/search=174`, `/api/v1/submit=152`, `/top=95`, `/score=80`, `/digest=66`, `/newest=66`, `/site/manifest.ly=63`.
- Aggregate referrers, 168 hours: `google.com=542`, canonical and alias referrers remain material, `/score=104`, `nothumansearch.fly.dev=62`, `/top=41`, `/site/chainray.online=38`, `/mcp-servers=33`, `/site/xquik.com=32`, `/mcp=31`, `/submit=28`, `hermesatlas.com=26`, `aurelianflo.com=25`.
- External referrer check: `https://hermesatlas.com/` returned HTTP 200 with title `Hermes Atlas - community map of Hermes Agent (100+ tools)`.
- Aggregate MCP analytics, 7 days: `tools/list=166457`, `initialize=24898`, `tools/call=259`; top tool calls include `search_agents=144`, `get_site_details=35`, `get_stats=20`, `check_url=18`, `get_top_sites=10`, `find_mcp_servers=8`, `recent_additions=8`, `verify_mcp=7`, `list_categories=5`, `submit_site=4`.
- Aggregate MCP client and directory signals include Cherry Studio, Claude Code, `MCP-Catalog-Bot`, `MCPScoringEngine`, `mcp-verify`, and `AgenstryBot`.
- Aggregate MCP query themes still include Hermes, agent skills, skill stores, scientific agent skills, agent earnings, document indexing, hardware/IoT, Home Assistant, and local/news lookups.
- Discovery surface checks: `/llms.txt`, `/.well-known/mcp.json`, `/.well-known/agent.json`, `/.well-known/commerce.json`, `/.well-known/ai-plugin.json`, `/api/v1`, `/api/v1/catalog`, `/openapi.yaml`, `/score`, `/monitor`, `/report`, and `/mcp` returned HTTP 200. `/.well-known/agent-card.json` returned HTTP 404, so strict A2A Agent Card claims remain blocked.
- Public `ai-tools` examples include Foundry-owned dogfood entries `8bitconcepts.com`, `bringyour.ai`, and `nothumansearch.ai`; label them before using that top list as evidence.

## Segment

The fresh signal is a third-party Hermes Atlas referrer plus continued Hermes, agent-skills, MCP catalog, and desktop-client activity. Treat it as an agent-directory and agent-builder channel fit signal, not as partnership, endorsement, customer demand, or A2A support.

The useful owner/channel route:

1. Agent-directory readers land on `/mcp`, `/mcp-servers`, `/.well-known/mcp.json`, `/llms.txt`, `/openapi.yaml`, and `/api/v1`.
2. Agent builders use `search_agents`, `get_site_details`, `check_url`, `get_top_sites`, and `recent_additions`.
3. Site owners who discover their profile should route to `/score` and free `/monitor`.
4. High-score profiles should route to report sharing, badge proof, and drift monitoring.
5. Partial-score profiles should route to `/score` first, then score-fix remediation only when public evidence shows missing readiness surfaces.

## Draft Channel Angle

Agent directories are starting to behave like source maps for the agentic web.

Not Human Search fits that workflow because it exposes both sides of the contract:

1. MCP and API surfaces for agents that need to search the agent-ready web.
2. Public score, report, badge, and monitor surfaces for site owners that need their own machine-readable footprint to stay stable.

Current corpus: 4,171 indexed sites with an average score of 35. The live MCP server exposes 11 tools, including search, site details, on-demand checks, MCP verification, top sites, recent additions, and monitor registration.

No paid placement. No ranking bypass. No A2A claim while the Agent Card route is still missing.

## Gated Test

Prepare exactly one gated agent-directory, agent-builder, Hermes-adjacent, MCP-client, or agent-skills channel touch using this packet.

Before external use, refresh `/api/v1/stats`, `/api/v1/categories`, `/mcp`, `/mcp-servers`, `/llms.txt`, `/openapi.yaml`, `/api/v1`, `/api/v1/catalog`, `/api/v1/quote`, `/api/v1/checkout`, `/.well-known/mcp.json`, `/.well-known/agent.json`, `/.well-known/agent-card.json`, `/.well-known/commerce.json`, `/.well-known/ai-plugin.json`, `/score`, `/monitor`, `/report`, representative `/site/{host}` profiles, high-score and partial-score `/fix/{host}` routes, aggregate MCP analytics, and aggregate traffic.

Verify the active Foundry/Owl-owned account identity for the selected channel, check `marketing/social-post-ledger.json` if present plus `outreach/distribution_log.csv` and sync-state public-action locks, and avoid `modelcontextprotocol/*` plus `punkpeye/*` surfaces from `unitedideas`.

## Guardrails

Do not imply Hermes Atlas, Hermes Agent, Cherry Studio, Claude Code, MCP Catalog, MCPScoringEngine, Agenstry, Xquik, ChainRay, Manifestly, or any listed domain is a customer, partner, endorsement, paid lead, monitor registration, badge-install consent, private demand, completed payment, revenue, framework-quality proof, agent-skill quality proof, source-code reliability, data freshness, uptime proof, seller certification, x402/ACP/MPP support for NHS, A2A support, paid ranking placement, preferred inclusion, or score-methodology bypass.
